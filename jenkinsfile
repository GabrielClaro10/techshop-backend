pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    dockerapp = docker.build("gabriel/techshop-backend", "-f ./vendor/laravel/sail/runtimes/8.0/Dockerfile ./")
                }
            }
        }
    }
}
