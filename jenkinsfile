pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    def dockerapp = docker.build("gabriel/techshop-backend", "-f src/Dockerfile src")
                }
            }
        }
    }
}