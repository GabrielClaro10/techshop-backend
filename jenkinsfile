pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    bat 'dir' 
                    docker.build("gabriel/techshop-backend", '-f ./app/Dockerfile .')
                }
            }
        }
    }
}
