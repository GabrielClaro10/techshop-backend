pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {

                    bat "docker build -t techshop-immagem ${dockerfilePath}"
                    
                    bat 'docker images'
                }
            }
        }
    }
}
