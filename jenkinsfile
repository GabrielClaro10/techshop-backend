pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    sh 'ls -l' // Lista os arquivos e diretórios
                    docker.build("gabriel/techshop-backend", '-f ./app/Dockerfile .')
                }
            }
        }
    }
}
