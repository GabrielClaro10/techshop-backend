pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                   dockerapp = docker.build("gabriel/techshop-backend", "-f src/Dockerfile src")
                }
            }
        }
    }
}