pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                   
                    def imageName = 'techshop-immagem'

                    bat "docker build -t ${imageName} ."

                    bat 'docker images'
                }
            }
        }
    }
}
