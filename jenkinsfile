pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    dockerapp = docker build ("gabrielclaro/techshop-backend", '-f ./Dockerfile')
                }
            }
        }
    }
}

