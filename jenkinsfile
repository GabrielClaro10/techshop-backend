pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    dockerapp = docker.build("gabriel/techshop", '-f ./app/Dockerfile ./app')
                }
            }
        }
    }
}
