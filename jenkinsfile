pipeline {
    agent any

    stages {
        stage('Build Image') {
            steps {
                script {
                    sh 'ls -l' // Executa o comando para listar arquivos e diretórios
                    docker.build("gabriel/techshop-backend", '-f ./app/Dockerfile .')
                }
            }
        }
    }
}
